-- =====================================================================
-- U11 领用+审批出库：领用单驳回意见落库（详设 U11 §6.4 / TBD-2）。
-- requisition 基线无独立驳回意见列，此处补充可空列（仅驳回时写入）。
-- =====================================================================
ALTER TABLE requisition ADD COLUMN reject_opinion VARCHAR(512);
